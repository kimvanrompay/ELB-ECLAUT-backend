{
  "name": "@lib/db",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "db:test:up": "docker compose -f docker-compose.test.yml up -d --build",
    "db:test:down": "docker compose -f docker-compose.test.yml down --volumes"
  },
  "private": true,
  "dependencies": {
    "dotenv": "^16.4.7",
    "knex": "^3.1.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "@types/pg": "^8.11.11",
    "typescript": "^5.7.3"
  }
}
