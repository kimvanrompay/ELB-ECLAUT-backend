{
  "name": "@lib/db",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "db:local:up": "docker compose -f docker-compose.local.yml up -d --build",
    "db:local:down": "docker compose -f docker-compose.local.yml down --volumes",
    "db:local:migrate": "sh ./scripts/migrate-db-local.sh localhost:5432 app example",
    "db:test:up": "docker compose -f docker-compose.test.yml up -d --build",
    "db:test:down": "docker compose -f docker-compose.test.yml down --volumes",
    "db:test:migrate": "sh ./scripts/migrate-db-test.sh localhost:5433 app example"
  },
  "private": true,
  "dependencies": {
    "knex": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.12.0",
    "@types/pg": "^8.11.11",
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "typescript": "^5.7.3"
  }
}
