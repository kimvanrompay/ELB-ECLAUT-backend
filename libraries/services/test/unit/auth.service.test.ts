// TODO: add tests for the auth service
describe('Auth Service', () => {
	describe('getJwtTokens', () => {
		it('should return JWT tokens', async () => {
			const email = 'admin@example.com';

			expect(true).toBe(true);
		});
	});
});
