// TODO: add tests for the login verification code service
describe('Auth Service', () => {
	describe('getNewLoginVerificationCode', () => {
		it('should return a new verification token', async () => {
			const email = 'admin@example.com';

			expect(true).toBe(true);
		});
	});
});
